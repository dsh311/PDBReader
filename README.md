# PDBReader
PDBReader scans a destination directory for assemblies and matches them to corresponding .pdb files in a source directory using signature-based comparison. It then copies the matching PDB files into the correct location alongside their associated assemblies.
